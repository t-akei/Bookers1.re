<h1>Editing Book</h1>

<%= form_with model: @book do |f| %>
  
  <h5>Title</h5>
  <%= f.text_field :title %>
  
  <h5>Body</h5>
  <%= f.text_area :body %><br>
  
  <%= f.submit "Update Book" %>
  
<% end %>
<span>
  <%= link_to 'Show', book_path(@book) %>
  |
  <%= link_to 'Back', books_path %>
</span>